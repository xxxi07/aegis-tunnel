# AEGIS-Tunnel — WireGuard-style config (Client)
# Usage: ./aegis-tunnel -c /etc/aegis/aegis.conf

[Interface]
PrivateKey = ~/.aegis-tunnel/private.key
Address = 10.0.0.2/24
Port = 9000
Mode = client

[Peer]
Endpoint = server.com:9000
# AllowedIPs = 10.0.0.0/24       ← 子网模式
AllowedIPs = 0.0.0.0/0           ← 全隧道模式

[Tunnel]
Keepalive = 30
Timeout = 10
